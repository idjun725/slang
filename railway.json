{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python -c \"print('=' * 60); print('[배포 시작] 서버 시작 명령 실행 중...'); print('=' * 60)\" && uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "source": {
    "rootDirectory": "backend"
  }
}

