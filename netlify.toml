[build]
  publish = "frontend"
  # 빌드 시 config.js 생성 (환경변수 주입)
  command = "node frontend/build-config.js"

# SPA 라우팅을 위한 리다이렉트는 frontend/_redirects 파일에서 처리

[build.environment]
  NODE_VERSION = "18"
  # API_BASE_URL은 Netlify 대시보드에서 환경변수로 설정하세요

